{let personName = "AnWar";
let lower = personName.toLowerCase();
let upper = personName.toUpperCase();
let titleCase = `${upper.slice(0,1) + lower.slice(1,5)}`;



console.log(lower);
console.log(upper);
console.log(titleCase)}